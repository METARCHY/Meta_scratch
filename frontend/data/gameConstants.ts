/**
 * @deprecated Import from '@/lib/modules/core' instead.
 * Kept for backward compatibility during migration.
 */
export {
    EVENTS,
    LOCATIONS,
    ACTION_CARDS,
    ALLOWED_MOVES,
} from '@/lib/modules/core/constants';

export { getConflicts } from './getConflicts';
